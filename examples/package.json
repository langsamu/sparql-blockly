{
  "private": true,
  "scripts": {
    "prestart": "npm install",
    "postinstall": "(cd typescript && npm install && npm run build)",
    "start": "http-server"
  },
  "devDependencies": {
    "http-server": "^13.0.1"
  }
}
