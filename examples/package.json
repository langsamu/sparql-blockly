{
  "private": true,
  "scripts": {
    "prestart": "npm install",
    "postinstall": "(cd typescript && npm install && npm run build)",
    "start": "http-server"
  },
  "devDependencies": {
    "http-server": "^14.1.0"
  }
}
